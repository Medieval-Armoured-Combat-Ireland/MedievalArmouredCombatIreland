import React from 'react';

import {MaciHero} from '@src/components/ui/maci-hero/maci-hero';

export const HomePage = (): React.ReactElement => {
    return (
        <div className="home-page">
            <MaciHero></MaciHero>
        </div>
    );
};
